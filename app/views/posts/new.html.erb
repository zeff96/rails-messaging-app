<section class="w-full h-screen flex flex-col p-3">
  <h2 class="text-red-600 font-bold text-2xl mb-3">Add new Post</h2>
  <%= turbo_frame_tag @post do %>
    <%= render "form", post: @post %>
  <% end %>
</section>